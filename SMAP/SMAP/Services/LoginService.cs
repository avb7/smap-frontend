using System;
namespace SMAP.Services
{
    public class LoginService
    {
        /*
        - LOGIN user (use Fb to get email, save in settings 
        - LOGOUT user, clear settings, nav to login 
        - CREATE user, user facebook, check if user exists, if no then create 
        */

        public LoginService()
        {
        }
    }
}
